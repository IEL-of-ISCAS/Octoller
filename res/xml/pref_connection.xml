<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->


    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <PreferenceCategory
        android:key="pref_category_wifi"
        android:title="@string/category_wifi" >
        <EditTextPreference
            android:defaultValue="10.0.0.90"
            android:key="@string/presf_key_wifi_server_ip"
            android:summary="@string/pref_wifi_server_ip" />
    </PreferenceCategory>
    <PreferenceCategory
        android:key="@+id/pref_category_bluetooth"
        android:title="@string/category_bluetooth" >
        <ListPreference
            android:dialogTitle="@string/pref_bt_choose_pair_device"
            android:key="@string/prefs_key_bt_target_device"
            android:title="@string/pref_bt_choose_pair_device" />
    </PreferenceCategory>

</PreferenceScreen>